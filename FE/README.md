# Frontend — Angular 21

Ver el [README principal](../README.md) y las reglas en [AGENTS.md](../AGENTS.md).

```bash
npm install
npm start           # :4200, proxy /api → http://localhost:8080 (proxy.conf.json)
npm run test:ci     # Vitest una vez
npm run build       # build de producción (dist/FE/browser, lo usa el Dockerfile)
```
